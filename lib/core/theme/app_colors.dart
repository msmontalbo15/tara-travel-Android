import 'package:flutter/material.dart';

class AppColors {
  // ── Brand Primary ────────────────────────────────────────────
  static const Color primary = Color(0xFFD85A30);       // Coral
  static const Color primaryLight = Color(0xFFF0997B);   // Light Coral
  static const Color sand = Color(0xFFFAECE7);           // Sand — chip/badge bg
  static const Color amber = Color(0xFFEF9F27);          // Sunset — accents
  static const Color deepEarth = Color(0xFF2C1A14);      // Deep Earth — dark surfaces
  static const Color darkAccent = Color(0xFF993C1D);     // Tagline / chip text
  static const Color warmMuted = Color(0xFFB4B2A9);      // Section labels, captions

  // ── Surfaces ─────────────────────────────────────────────────
  static const Color surfaceDark = Color(0xFF1A0A04);    // Hero headers
  static const Color surfaceLight = Color(0xFFF7F4F0);   // Warm White — page bg
  static const Color cardWhite = Color(0xFFFFFFFF);
  static const Color background = Color(0xFFFAECE7);     // Sand

  // ── Text ─────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF888888);
  static const Color muted = Color(0xFF8E8E93);

  // ── Borders & Dividers ───────────────────────────────────────
  static const Color cardBorder = Color(0xFFE8E8E8);
  static const Color dividerLight = Color(0xFFF0F0F0);

  // ── Chips & Badges ───────────────────────────────────────────
  static const Color chipBackground = Color(0xFFFAECE7); // Sand
  static const Color chipText = Color(0xFF993C1D);        // Dark accent

  // ── Semantic ─────────────────────────────────────────────────
  static const Color green = Color(0xFF3B6D11);
  static const Color greenBg = Color(0xFFEAF3DE);
  static const Color greenLight = Color(0xFFD1FAE5);
  static const Color greenBright = Color(0xFF10B981);
  static const Color blue = Color(0xFF3B82F6);
  static const Color blueLight = Color(0xFFDBEAFE);
  static const Color purple = Color(0xFF8B5CF6);
  static const Color purpleLight = Color(0xFFEDE9FE);
  static const Color red = Color(0xFFEF4444);
  static const Color redLight = Color(0xFFFEE2E2);
  static const Color amberLight = Color(0xFFFEF3C7);
  static const Color amberBg = Color(0xFFFFF8ED);
  static const Color amberText = Color(0xFF854F0B);
  static const Color teal = Color(0xFF14B8A6);

  // ── Category ─────────────────────────────────────────────────
  static const Color catAccommodation = Color(0xFF3B82F6);
  static const Color catFood = Color(0xFFEF9F27);
  static const Color catActivities = Color(0xFF10B981);
  static const Color catTransport = Color(0xFFD85A30);
}
